mutation InsertMultipleTemperatures {
  insert_runtime_verification(
    objects: [
      {temperature: "3.1"},
      {temperature: "3.2"}
    ]
  ) {
    affected_rows
  }
}

Add a column for record_time (or some other variable that can be used for matching with simulation data)
